  There are two Delphi projects. One (SpaceShip.dpr) is just the launcher. The second (AsterdoidField.dpr) is the real thing.

  I've attached modified versions of two GLScene units:

- joystick.pas  : deal with R-axis and POV button
- GLobjects.pas : RayCastIntersect of the tGLAnnulus object

